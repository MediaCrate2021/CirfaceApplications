[build]
builder = "NIXPACKS"
buildCommand = "npm run build -w apps/estimator"
watchPatterns = ["apps/estimator/**", "packages/core/**"]

[deploy]
startCommand = "npm run start -w apps/estimator"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
